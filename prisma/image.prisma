model Image {
  id            Int    @id @default(autoincrement())
  url           String
  presignedUrl  String?
  postId        Int
  post          Post   @relation(fields: [postId], references: [id], onDelete: Cascade)
  @@schema("post")
}